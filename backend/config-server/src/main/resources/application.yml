server:
  port: 8888

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://gitlab-sys.onetech-group.corp/automatisation-pipeline
          default-label: main
          clone-on-start: true
          username: Malek.khemiri
          password: ${GITLAB_PASSWORD}
eureka:
  client:
    service-url:
      defaultZone: http://dc-discovery:8761/eureka/
  instance:
    prefer-ip-address: true
management:
  endpoints:
    web:
      exposure:
        include: health,info
